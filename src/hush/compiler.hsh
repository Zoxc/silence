use "lexer"
use "parser"
use "error"
use "region"

struct Compiler
	region Region.Base
	lexer Lexer
	parser Parser
	errors Array(*Error)

	action create(file) :
		lexer <- Lexer(&self, file),
		parser <- Parser(&lexer)