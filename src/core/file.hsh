struct File
	shared fn read(path String)
		.f = path.c_str -> |p| C.fopen(p, "r")

		if is_nil_ptr(f)
			return Option.Nil()

		.result String

		loop
			.c = C.fgetc(f)
			if c == -1
				break
			result ~= String.from_c(force_cast c)

		C.fclose(f)

		return Option.Some(result)
