struct IO
	shared fn print(s String)
		Runtime.hush_puts(&s)

	shared fn puts(s String)
		print s
		.l = 10
		print String.from_data(&l, 1)

	shared fn gets()
		.r = undef()
		Runtime.hush_gets(&r)
		return r